# 원본 사진 (용량이 커서 저장소에 올리지 않습니다 — 회사 공유드라이브에 보관)
/source/

# 빌드 산출물 (tools 폴더에서 npm run build 로 다시 만들 수 있습니다)
/photos/
/thumbs/
/articles/
/videos/
/downloads/
/assets/data.js

# 기사 PDF 중간 산출물 (npm run articles 로 다시 만들 수 있습니다)
/articles-pdf/

# 작업 임시 파일
/.build-tmp/
/.chrome-profile/
node_modules/
